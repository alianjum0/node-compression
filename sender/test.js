const chai = require('chai');
const transmit = require('./transmit')

chai.should();
chai.use(chaiHttp);

describe('Testing Packets', () => {
  describe('It should test packet type 1', () => {
    it('It should retrun string in packet format 1', (done) => {
    });
  });
});
